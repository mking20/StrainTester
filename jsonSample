import json

FILENAME = "sampleObjects.json"
jsonSampleString = 
'{
  "sampleNumber": "25",
  "timePassed": "24:01:285",
  "strain" : 23.42",
  "displacement" : "0.23"
}'

##
## Function that converts to json objects (dictionaries)
##

def readStrainTesterFile():
    with open(FILENAME, 'r'):
        sampleList = []
        timeStart = 0
        sampleNumber = 0
    for i in range(50):
      x,y = random.randint()
      sample = CreateJSONSample(x,y,sampleNumber,timePassed)
      sampleList.append(sample)
      sampleNumber +=1

 return sampleList
