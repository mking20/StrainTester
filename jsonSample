import json

FILENAME = "sampleObjects.json"
jsonSampleString = 
'{
  "sampleNumber": "25",
  "timePassed": "24:01:285",
  "strain" : 23.42",
  "displacement" : "0.23"
}'

## Function that converts to json objects (dictionaries)

def createJSONSample(sensor1,sensor2):
  createJson = 
  {
    "x": "sensor1", 
    "y" : "sensor2", 
    "sampleString" : [{"jsonSampleString": ["sampleNumber","timePassed"]}]
  }

  for i in sample():
    createJson.append(i)
    sampleNumber +=1
    print(createJson)
 
 return (createJson)
  
def readStrainTesterFile():
  with open(FILENAME, 'r'):
    sampleList = []
    timeStart = 0
    sampleNumber = 0
    for i in range(50):
      x,y = random.randint()
      sample = createJSONSample()
      sampleList.append(sample)
      print(sampleList)
      
 return sampleList
